<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"

    android:layout_height="wrap_content"
    android:layout_width="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <ImageView
            android:id="@+id/im_message"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@mipmap/ic_launcher"
            android:scaleType="fitCenter"
            android:layout_margin="5dp"
            android:layout_alignParentLeft="true"/>
        <TextView
            android:id="@+id/txt_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/im_message"
            android:textSize="20sp"
            android:text="MyMessage"
            android:layout_above="@+id/remove_message"
            android:layout_alignTop="@+id/im_message" />
        <ImageButton
            android:id="@+id/remove_message"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_delete_black_24dp"
            android:layout_alignParentRight="true"
            android:layout_alignBottom="@id/im_message"
            android:elevation="8dp" />

        <ImageButton
            android:id="@+id/edit_message"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_border_color_black_24dp"
            android:elevation="8dp"
            android:layout_alignBottom="@+id/remove_message"
            android:layout_toLeftOf="@+id/remove_message"
            android:layout_toStartOf="@+id/remove_message"
            android:layout_alignTop="@+id/remove_message" />
    </RelativeLayout>

</android.support.v7.widget.CardView>